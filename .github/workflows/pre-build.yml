name: Pre-build checks

on:
  push:
    branches: [ feature/first-bit]
  pull_request:
    branches: [ feature/first-bit]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        run: echo "Success"
